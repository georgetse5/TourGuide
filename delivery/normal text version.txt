Changes in build.gradle.kts 

The build.gradle.kts file has been modified to update the Android Gradle plugin version from "8.1.3" to "8.1.4".

Dependencies have been added to the build.gradle.kts file.

implementation("com.google.android.gms:play-services-maps:18.2.0")
implementation("com.google.android.gms:play-services-location:21.0.1")
implementation("com.squareup.retrofit2:retrofit:2.9.0")
implementation("com.squareup.retrofit2:converter-gson:2.9.0")
implementation("com.google.maps.android:android-maps-utils:2.2.1")
implementation("com.google.maps:google-maps-services:0.15.0")


New Changes in ActivityOne.java (now ExploreFragment.java):

The file has been renamed from ActivityOne.java to ExploreFragment.java.
The class has been updated to extend Fragment instead of AppCompatActivity.
The onCreate method has been replaced with onCreateView for a Fragment.
Context is now obtained using requireContext().
The onDestroy() method is now public and overridden for Fragment lifecycle.
Permission requests are made using requireActivity() for the fragment.
The layout is inflated using inflater.inflate(R.layout.activity_one, container, false).
Markers are added using MyMap.addMarker(markerOptions).
Toast messages are displayed using Toast.makeText(requireContext(), ...).
Location Manager and Places are initialized using requireContext() and createClient(requireContext()) for the fragment.
Added marker click handling logic to initiate directions and draw polylines.
Introduced the OnDirectionsResultListener interface for handling the results of directions requests.
Updated the getDirections, drawPolyline, and drawPolyline1 methods to incorporate directions functionality.
Modified the onMapReady method to set a click listener for the map.
Implemented the logic for saving a place to the database.
Updated the getLocation method to handle location permissions and settings.
Enhanced the requestNewLocationData method to update the current location.

New Changes in ActivityFour.java (now FavoritesFragment.java):

The file has been renamed from ActivityFour.java to FavoritesFragment.java.
The class has been updated to extend Fragment instead of AppCompatActivity.
The onCreate method has been replaced with onCreateView for a Fragment.
Context is now obtained using requireContext().
The layout is inflated using inflater.inflate(R.layout.activity_four, container, false).
The RecyclerView is now accessed through the inflated view using findViewById.
The apiCalls method now uses requireContext() when creating the Volley RequestQueue.
The mRecyclerView and layoutManager are now initialized using requireContext().

New Changes in MainActivity.java:

The MainActivity.java file has been modified.
The class now includes fragments declarations for ExploreFragment, RestaurantsFragment, SightsFragment, and FavoritesFragment.
The onCreate method has been updated to include fragment initialization and handling for BottomNavigationView.
The bottom navigation item selection is now handled using a switch case for fragment loading.
The buttons now launch corresponding fragments (ExploreFragment, RestaurantsFragment, SightsFragment, FavoritesFragment) instead of activities.
A helper method loadFragment(Fragment fragment) has been added to streamline fragment loading.

Changes in ActivityTwo.java (now RestaurantsFragment.java):

The file has been renamed from ActivityTwo.java to RestaurantsFragment.java.
The class has been updated to extend Fragment instead of AppCompatActivity.
The onCreate method has been replaced with onCreateView for a Fragment.
Context is now obtained using requireContext().
The layout is inflated using inflater.inflate(R.layout.activity_two, container, false).
The RecyclerView is accessed through the inflated view using inflate.findViewById(R.id.recyclerview).
The recyclerView.setLayoutManager is now initialized with new LinearLayoutManager(requireContext()).
The adapter for the recyclerView is set using new MyAdapter(requireContext(), items).

Changes in ActivityThree.java (now SightsFragment.java):

The file has been renamed from ActivityThree.java to SightsFragment.java.
The class has been updated to extend Fragment instead of AppCompatActivity.
The onCreate method has been replaced with onCreateView for a Fragment.
Context is now obtained using requireContext().
The layout is inflated using inflater.inflate(R.layout.activity_three, container, false).
The RecyclerView is accessed through the inflated view using inflate.findViewById(R.id.recyclerview).
The recyclerView.setLayoutManager is now initialized with new LinearLayoutManager(requireContext()).
The adapter for the recyclerView is set using new MyAdapter(requireContext(), items).

Changes in activity_four.xml:

The file has been updated to reference the FavoritesFragment instead of ActivityFour.
The tools:context attribute in the root layout now points to .FavoritesFragment instead of .ActivityFour.

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".FavoritesFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/my_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp"
        android:scrollbars="vertical"
        android:layout_above="@+id/bottom_navigation"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"/>
    
    <!-- Other layout elements or views can be added as needed -->

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        app:menu="@menu/bottom_navigation_menu"/>
</RelativeLayout>



Changes in activity_main.xml:

Added a FrameLayout with the ID fragment_container to act as the container for fragment transactions.
Set the visibility of the existing ImageView with ID logoImageView to "gone" by default.
Added the BottomNavigationView with the ID bottom_navigation to the layout.

<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- Other existing layout elements or views can be found here -->

    <ImageView
        android:id="@+id/logoImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="30dp"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navigation"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:visibility="gone"/>
        
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navigation"/>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="0dp"
        android:background="@color/white"
        app:labelVisibilityMode="labeled"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:menu="@menu/bottom_navigation_menu"/>
</androidx.constraintlayout.widget.ConstraintLayout>


Changes in activity_one.xml:

Updated the tools:context attribute to reference the ExploreFragment instead of the old ActivityOne.

<?xml version="1.0" encoding="utf-8"?>
<fragment
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ExploreFragment"
    android:name="com.example.tourguide.ExploreFragment"
    android:id="@+id/fragment"
    tools:layout="@layout/fragment_explore"/>


Changes in activity_three.xml:

Updated the tools:context attribute to reference the SightsFragment instead of the old ActivityThree.

<?xml version="1.0" encoding="utf-8"?>
<androidx.recyclerview.widget.RecyclerView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/recyclerview"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SightsFragment"
    android:paddingTop="?android:attr/actionBarSize"/>


Changes in activity_two.xml:

Updated the tools:context attribute to reference the RestaurantsFragment instead of the old ActivityTwo.

<?xml version="1.0" encoding="utf-8"?>
<androidx.recyclerview.widget.RecyclerView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/recyclerview"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".RestaurantsFragment"
    android:paddingTop="?android:attr/actionBarSize"/>

Changes in bottom_navigation_menu.xml:

Created a new XML file for the bottom navigation menu.

<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/navigation_explore"
        android:title="Explore"
        android:icon="@drawable/ic_explore" />

    <item
        android:id="@+id/navigation_restaurants"
        android:title="Restaurants"
        android:icon="@drawable/ic_restaurants" />

    <item
        android:id="@+id/navigation_sights"
        android:title="Sights"
        android:icon="@drawable/ic_sights" />

    <item
        android:id="@+id/navigation_favorites"
        android:title="Favorites"
        android:icon="@drawable/ic_favorites" />

</menu>

Changes in themes.xml:

Modified the colorPrimary attribute to use the pale_olive color.

<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.TourGuide" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/pale_olive</item>
    </style>

    <style name="Theme.TourGuide" parent="Base.Theme.TourGuide" />
</resources>



New File: DirectionsApiService.java

package com.example.tourguide;

import retrofit2.Call;
import retrofit2.http.GET;
import retrofit2.http.Query;

public interface DirectionsApiService {
    @GET("maps/api/directions/json")
    Call<DirectionsResponse> getDirections(
        @Query("origin") String origin,
        @Query("destination") String destination,
        @Query("key") String apiKey
    );
}


New File: DirectionsResponse.java

package com.example.tourguide;

import java.util.List;

public class DirectionsResponse {
    public List<Route> routes;
}



Changes Made to OnDirectionsResultListener.java:

Created a new interface OnDirectionsResultListener to handle the results of directions requests.
Added two methods:
onDirectionsResult(List<LatLng> polylinePoints): Invoked when directions result is successfully obtained, providing a list of polyline points.
onDirectionsFailure(): Invoked when there is a failure in obtaining directions.


Changes Made to Polyline.java:

Created a new class Polyline to represent polyline data.
Added a field points to store the polyline points.


Changes Made to Route.java:

Created a new class Route to represent route data.
Added a field overview_polyline of type Polyline to store the overview polyline data.
